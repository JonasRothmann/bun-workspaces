{
  "name": "bun-workspaces",
  "module": "index.ts",
  "type": "module",
  "bin": {
    "bun-workspaces": "dist/index.js"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "commander": "^11.0.0"
  },
  "scripts": {
    "build": "bun build --target=bun ./index.ts --outdir=dist",
    "clean": "rm -rf dist"
  }
}
